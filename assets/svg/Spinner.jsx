import React from "react";
import classes from "../../components/Helper/style/modal.module.scss";

const Spinner = () => {
  return <span className={classes.spinner}></span>;
};

export default Spinner;
